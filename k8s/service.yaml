apiVersion: v1
kind: Service
metadata:
  name: app-registro-svc
  namespace: production
  labels:
    app: app-registro
spec:
  selector:
    app: app-registro
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
